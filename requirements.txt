# Core dependencies
tqdm>=4.66.0
psutil>=5.9.0
colorama>=0.4.6
typing-extensions>=4.8.0

# GUI
ttkbootstrap>=1.10.1

# System and concurrency helpers
pillow>=10.0.0     # If you ever display thumbnails or icons
pywin32>=306; sys_platform == "win32"

# Optional utilities
requests>=2.31.0   # In case plugin or updater needs it later
